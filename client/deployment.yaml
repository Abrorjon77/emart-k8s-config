apiVersion: apps/v1
kind: Deployment
metadata:
  name: client
  namespace: emart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
      - name: client
        image: abroor/emart-client:a7452a535831f15abc4fd015086454dc1bf1c23c
        ports:
        - containerPort: 4200
---
apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: emart
spec:
  selector:
    app: client
  ports:
  - port: 4200
    targetPort: 4200
